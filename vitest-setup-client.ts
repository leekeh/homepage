/// <reference types="@vitest/browser/matchers" />

// Client (browser) test setup. `vitest-browser-svelte` auto-unmounts rendered
// components after each test, so this file only needs to exist for shared
// browser-mode setup. Add global mocks or CSS imports here if a widget test
// ever needs them.
export {};
