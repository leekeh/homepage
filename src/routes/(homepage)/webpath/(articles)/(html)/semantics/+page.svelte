Do not use elements as other elements. For example, do not use a button element to navigate to a
different page. Do not use table headers outside of a table. Etc. Think about the layout of your
document, structure it. Nesting of headings. sections, aside elements, etc. There are a few elements
that only exist to structure the page semantically. Find the error
